# Base URL of the Bank_System backend.
# Leave empty to use the Vite dev proxy (recommended in dev): requests go to /api and are
# forwarded to VITE_API_TARGET below. Set an absolute URL for production builds.
VITE_API_URL=

# Where the dev proxy forwards /api requests (dev only).
VITE_API_TARGET=http://localhost:8080
